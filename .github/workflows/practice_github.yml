name: CI

on:
  workflow_dispatch:

jobs:
  build:
    runs-on: E-5CG5321YYZ

    steps:
      - uses: actions/checkout@v4

      - name: CMD Test
        shell: cmd
        run: |
          echo Hello World
          echo Second Line
